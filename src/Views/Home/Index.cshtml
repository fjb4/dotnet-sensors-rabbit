@model IEnumerable<Guid>
@{
    ViewData["Title"] = "Sensor Publisher";
}

<div class="text-center">
    <h1 class="display-4">Sensor Publisher</h1>
    @if (Model != null && Model.Any())
    {
        <h2>Sensor Ids</h2>
        @foreach (var sensorId in Model)
        {
            <p>@sensorId.ToString()</p>
        }
    }
</div>
